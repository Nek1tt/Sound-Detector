# ══════════════════════════════════════════════════════════════════════════
# requirements.txt
# Совместимо с: x86_64 Linux/macOS/Windows И Raspberry Pi OS Lite (Bookworm 64-bit)
#
# Установка на обычном компьютере:
#   pip install -r requirements.txt
#
# Установка на Raspberry Pi:
#   ./scripts/setup_pi.sh          # рекомендуется
#   — или вручную —
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# ══════════════════════════════════════════════════════════════════════════

# ── PyTorch CPU-only ───────────────────────────────────────────────────────
# Примечание: на aarch64 (Pi) нужен --extra-index-url (см. setup_pi.sh).
torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0

# ── Аудио ─────────────────────────────────────────────────────────────────
librosa==0.11.0
soundfile==0.13.1

# ── Данные и ML ───────────────────────────────────────────────────────────
numpy>=2.0.2
pandas>=2.2.2
scikit-learn>=1.5.0
scipy>=1.13.1

# ── Кластеризация и визуализация ──────────────────────────────────────────
matplotlib==3.10.0
umap-learn==0.5.12
pacmap==0.9.1
faiss-cpu==1.13.2

# ── Прогресс-бар ──────────────────────────────────────────────────────────
tqdm==4.67.3

# ── Микрофон (опционально, для daemon --mode mic) ─────────────────────────
# На Pi нужен предварительно: sudo apt install -y portaudio19-dev
# sounddevice==0.4.7